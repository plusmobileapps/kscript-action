name: 'Kscript CI for Unit Tests'
description: 'Run the unit tests in a kscript project'
inputs:
  kts-file:  # id of input
    description: 'the path to the kts file in your repository i.e. hello-world.kts'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.kts-file }}